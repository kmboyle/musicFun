import { DemoDirective } from './demo.directive';
import { ElementRef } from '@angular/core';

describe('DemoDirectiveDirective', () => {
  it('should create an instance', () => {
    // const el: ElementRef;
    // const directive = new DemoDirective(el);
    // expect(directive).toBeTruthy();
  });
});
